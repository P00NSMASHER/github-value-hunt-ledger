# Step 0 Resume Handoff

Read these files first, in this order:

1. `portfolio_prework/PORTFOLIO_BUILD_STATE.json`
2. `portfolio_prework/ARCHITECTURE_EVIDENCE_BUNDLE.json`
3. `portfolio_prework/PROJECT_ID_REGISTRY.json`
4. `portfolio_prework/AUTONOMY_MATRIX.json`

Then run the SHA-based delta detector before broad inspection.

The 10-step parallel prework is complete. It already established repository scope, stable project IDs, a repository snapshot generator, SHA delta detection, durable build state, autonomy boundaries, workflow inventory, test inventory, dependency inventory, and this evidence bundle.

Use the evidence bundle to avoid repeating reconnaissance already captured at exact SHAs. Inspect changed repositories/files only unless a specific architecture question requires more evidence.

The interactive architecture workflow remains unchanged: complete Step 0, verify it, commit it, update durable state, report, then stop and wait for `CONTINUE`.
