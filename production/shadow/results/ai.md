# AI / agent infrastructure shadow results

Append-only shadow log. These findings are NOT authoritative MASTER promotions.

## Run 1 — 2026-09-20

HYPOTHESIS: Durable agent runtimes become materially more valuable when every external effect is forced through a recorded host boundary with strict deterministic replay, fail-loud divergence, crash resume, approval pauses, and single-writer state protection. A runtime that can turn a recorded run into a no-live-effects regression fixture should materially compress long-horizon reliability and verification engineering compared with graph/checkpoint-only frameworks.

DISCOVERY METHODS: (1) direct domain search for durable/resumable agent runtimes and checkpoint/replay systems; (2) code-level search around `checkpoint`, `resume`, `call log`, `host call`, replay divergence, approvals, strict verification, and run leases; (3) ecosystem/comparator search across deterministic-replay and workflow/runtime families, including Atmosphere's multi-runtime AI governance layer; (4) recent commit/history archaeology for reliability defects, release failures, CI hardening, and concurrency fixes. Specialist decomposition: CODE INSPECTOR traced the replay journal, source/argument divergence and strict fixture semantics; RELIABILITY/ECOSYSTEM analyst checked lease/storage behavior and compared adjacent runtimes; COMMERCIAL ANALYST scoped a reliability-retrofit wedge; independent RED-TEAM/VERIFIER reviewed the frozen evidence without using the proposed score and attempted to reduce the thesis to commodity workflow durability.

BEST CANDIDATE: `ThousandBirdsInc/chidori`
CANONICAL URL: https://github.com/ThousandBirdsInc/chidori
EXACT REVISION: `223bb8779f63822c3e63a9a4347dda7483a02158`
EVIDENCE SNAPSHOT: `shadow-ai-20260920-chidori-223bb877`
VERIFIER VERDICT: PASS_WITH_LIMITS
PROPOSED SCORE: A4 B5 C5 D5 E4 F4 = 27/30. This is a shadow proposal only, not a MASTER promotion.

CLAIM / EVIDENCE PACKET:
- IMPLEMENTED + TESTED: durability is based on deterministic replay of an effect journal rather than only process-memory/checkpoint restoration. Host effects are deterministically keyed, live results are journaled, and restore re-evaluates the bundle while feeding prior results until the live frontier. `crates/chidori-js/src/replay.rs` explicitly detects bundle changes and treats edits to already-journaled behavior as divergence rather than silently substituting new side effects.
- IMPLEMENTED + TESTED: `crates/chidori-js/tests/replay.rs` contains concrete record→fresh-runtime replay, suspend→persist→restore→resume, post-frontier modify-and-resume, pre-frontier fail-loud divergence, memoized deterministic-step replay, and independent-restored-runtime tests. The replay test deliberately avoids re-performing previously recorded effects; the divergence test errors when an already-executed host call changes.
- IMPLEMENTED: the strict verification workflow in `docs/replay.md` requires a source snapshot manifest, disables providers/tools, uses an untrusted policy posture, refuses source drift, enables strict replay so a journal miss cannot silently execute live, ignores the lax-replay escape hatch, requires the full journal to be consumed, refuses pauses/incomplete runs, detects unexpected live calls, and checks byte-identical output. Export refuses incomplete/leased/paused runs.
- IMPLEMENTED: crash continuation preserves the original run ID, persisted host promises, approvals, VFS, signals and policy state. Server resume/signal/approve paths use a run lease and return 409 Conflict to a second writer before it mutates durable state.
- IMPLEMENTED: powerful tool/network/workspace effects are approval-gated by default; unattended untrusted execution fails closed unless the operator explicitly selects trusted mode.
- PROVENANCE: root license is Apache-2.0. The inspected head is the 2026-08-30 v3.8.1 release-fix commit.
- HISTORY / NEGATIVE EVIDENCE: the same head documents that v3.8.0 shipped npm/PyPI packages while crates.io/binary publication failed due to an engine `TypeofBr` panic plus an out-of-sync internal crate, and that main CI had accumulated unrelated failures. v3.8.1 adds regression and pre-publish drift checks, but this is evidence of a young runtime with real release-engineering risk, not a mature infallible substrate.

CLAIMS NOT VERIFIED / LIMITS:
- Distributed single-writer safety is backend-dependent. The repository documentation says run leases are advisory on filesystem and S3 stores; stronger exclusion is available on stores such as SQLite/cell-store/Durable Objects. Do not claim universal exactly-once execution from the lease feature.
- The replay guarantee only covers effects mediated through the runtime boundary. Workspace state remains real disk; recorded writes can be re-materialized during verification, and external behavior bypassing the host boundary cannot be magically replayed.
- Normal replay has an explicit `CHIDORI_REPLAY_LAX=1` compatibility escape hatch for argument divergence; the dedicated `chidori verify` path is stricter and ignores that escape hatch.
- I did not independently rerun the repository test suite. A local clone/test setup attempt was blocked by the execution sandbox's lack of outbound DNS, so TESTED means committed tests were inspected rather than rerun here.
- Production-scale throughput, multi-region durability, compatibility with arbitrary Node/Python agent ecosystems, and realized customer reliability savings were not independently established.

INDEPENDENT RED-TEAM / VERIFIER: PASS_WITH_LIMITS. The strongest commodity objection is that Temporal, DBOS, AWS durable-execution systems and agent checkpoint frameworks already solve durable workflows or resumable state. Chidori's non-commodity value is narrower: an agent-specific effect boundary plus zero-model-call deterministic replay, strict source/argument divergence, a no-live-effects CI fixture contract, human pause/resume, and explicit single-writer continuation. The thesis should therefore be “replayable agent execution and verification,” not merely “durable agents.” The strongest operational objection is the custom embedded JavaScript runtime and backend-dependent lease semantics; migration/integration burden could erase the benefit for teams already deeply invested in Temporal/LangGraph-style stacks. Recent release/CI defects lower evidence/operational confidence and prevent an unqualified PASS.

COMMERCIAL WEDGE: Sell a fixed-price **Replayable Agent Reliability Retrofit** to AI platform/application teams operating a high-value tool-using agent. Wrap one workflow's model/tool/API effects behind the runtime boundary; deliver strict replay fixtures, crash/resume and approval-gate tests, source-drift evidence, and duplicate-driver tests. The money path is lower incident MTTR, less repeated model spend during debugging, reduced duplicate side-effect risk after crashes/retries, and faster deterministic regression testing. A first paid engagement appears feasible on a weeks-scale integration rather than requiring a full platform replacement, but customer migration cost is the gating variable.

COMPARATOR: `Atmosphere/atmosphere@5eaac40919357742ac0d67c59840d0b6eb909d53` provides independent adjacent evidence that agent-runtime value is moving toward portable governance, capability contracts, approvals, passivation/cancellation, signed runtime capability manifests and fail-closed safety across many AI frameworks. It is broader than Chidori, while Chidori's inspected evidence is deeper on deterministic side-effect replay and recorded-run verification. Atmosphere was used as a comparator, not independently promoted in this run.

VALUE HANDOFF:
1. CAPABILITY DELTA: a reusable deterministic agent-run evidence primitive becomes available: journal every mediated side effect, reconstruct a run without live external actions, fail on source/argument drift, and continue safely at the frontier after crash or human pause.
2. GRAPH EDGE: directly strengthens the production architecture's restart/recovery, verifier reproducibility, approval gating and lease-recovery requirements. It could serve as a candidate worker-runtime primitive if its backend and execution-model limitations survive a dedicated integration test.
3. RADAR SIGNAL: deterministic replay is converging from generic durable-workflow systems into agent-specific runtimes where model/tool calls are first-class recorded effects; Atmosphere independently shows adjacent convergence around governance/capability contracts.
4. EXPERIMENT IMPACT: restart, lease-conflict, approval fail-closed, evidence mutation/source drift and no-live-effects regression tests become substantially cheaper to make falsifiable because a recorded run can act as a deterministic fixture.
5. COMMERCIAL IMPACT: specific buyer is an AI platform/application team with expensive nondeterministic tool-using workflows; first wedge is a reliability retrofit rather than wholesale orchestration replacement; moat comes from the combined effect journal + strict replay verifier + source/argument provenance + operational continuation semantics.
6. NEGATIVE KNOWLEDGE: “checkpoint/resume” alone is not evidence-grade durability. Future AI-runtime searches should require side-effect authority boundaries, fail-loud divergence, a strict no-live replay mode, crash/frontier tests, explicit human-approval semantics, and storage-specific concurrency guarantees before awarding strong reliability credit.

EFFORT: 4 materially different discovery modes; 2 deep inspections (Chidori and Atmosphere); approximately 20 external research calls; 0 reproducible test executions, with 1 local clone/test setup attempt blocked by sandbox DNS.

## Run 2 — 2026-09-20

HYPOTHESIS: The most important missing semantic in replayable agent runtimes is the **ambiguous-effect window**: once a non-idempotent external call may have committed but its acknowledgement was lost, a safe runtime must not infer failure and retry. A compact runtime that persists intent before I/O, distinguishes completed versus indeterminate outcomes, parks indeterminate writes for actor-attributed evidence, and keeps model approval authority outside the model can materially improve money-mutating or otherwise irreversible agent workflows even if it is not a full distributed scheduler.

DISCOVERY METHODS: (1) direct search for durable/resumable tool-calling agent runtimes with event sourcing, approvals, crash recovery and non-idempotent side-effect semantics; (2) code/phrase search around `TOOL_EXECUTION_STARTED`, `TOOL_OUTCOME_UNKNOWN`, `awaiting_recovery`, replay divergence, actor/evidence resolution and fail-closed planner parsing; (3) low-attention discovery deliberately emphasizing zero-star and newly published runtimes; (4) adjacent ecosystem comparison against deterministic record/replay libraries and independent durable-tool documentation; (5) commit-history archaeology to identify when ambiguity handling and model-planner hardening entered the candidate. Specialist decomposition: CODE INSPECTOR traced event folding, tool start/outcome ordering, approval authority and recovery; RELIABILITY ANALYST inspected journal/concurrency semantics and failure windows; COMMERCIAL ANALYST mapped the semantics to financial/customer-facing side effects; independent RED-TEAM/VERIFIER received the claim packet without the score and attempted to disprove novelty, multi-writer safety and evidence integrity.

BEST CANDIDATE: `prathamesh-git9/agent-runtime`
CANONICAL URL: https://github.com/prathamesh-git9/agent-runtime
EXACT REVISION: `3d4c3bf58b4262cf523e78655ff0bf1c9453830e`
EVIDENCE SNAPSHOT: `shadow-ai-20260920-agent-runtime-3d4c3bf5`
VERIFIER VERDICT: PASS_WITH_LIMITS
PROPOSED SCORE: A4 B4 C5 D4 E4 F3 = 24/30. This is a shadow proposal only, not a MASTER promotion.

CLAIM / EVIDENCE PACKET:
- IMPLEMENTED + TESTED: `src/agent_runtime/runtime.py` persists planner decisions and `TOOL_EXECUTION_STARTED` before entering the handler. Completed outcomes are replayed from the journal rather than re-executed. For a non-idempotent tool with a recorded start but no durable terminal outcome, the runtime emits `TOOL_OUTCOME_UNKNOWN` and returns `AWAITING_RECOVERY` instead of retrying.
- IMPLEMENTED + TESTED: `tests/test_durability.py::test_crash_after_non_idempotent_start_parks_for_evidence_not_retry` constructs the exact dangerous crash window, marks the simulated external email as possibly committed, advances the run twice, proves the side effect is not repeated, and requires `resolve_tool_outcome` with non-empty actor and evidence before continuation. Duplicate manual resolution is rejected.
- IMPLEMENTED + TESTED: cold restart uses a fresh runtime/planner against the same SQLite journal; the completed effectful tool is not re-executed and the replacement planner receives zero calls. Approval state likewise survives restart, and a denied approval becomes tool feedback rather than an execution bypass.
- IMPLEMENTED + TESTED: the model-backed planner cannot self-approve gated tools. Tests feed model output containing `approved: true` / `human_approved_by` fields and the runtime still stops before the tool. Malformed model output raises `PlannerError` instead of being converted into `RUN_COMPLETED` success.
- IMPLEMENTED: `src/agent_runtime/journal.py` is insert-only for events, uses `(run_id, seq)` as its primary key and a unique terminal-outcome index per call ID. Direct duplicate-sequence appends surface `ConcurrentAppend`; terminal tool outcomes cannot be silently duplicated in the same journal.
- HISTORY: commit `15dce4f2ec1cafb13b12e3010858323e1c409e28` is explicitly titled “Fail closed on ambiguous tool outcomes.” The inspected head `3d4c3bf58b4262cf523e78655ff0bf1c9453830e` then added the model-backed planner with tests designed so the model cannot weaken replay or approval invariants; its commit message says the suite grew from 33 to 48 tests.
- PROVENANCE: repository license is MIT. The repository is zero-star/zero-fork at inspection time, making it unusually low-attention relative to the specificity of the recovery semantics.

CLAIMS NOT VERIFIED / LIMITS:
- This is not distributed exactly-once execution. The SQLite implementation has no run lease/ownership epoch and the README explicitly says concurrent production workers, network partitions and multi-node scheduling are not proven. A `(run_id, seq)` uniqueness guard detects a direct append collision, but it is not sufficient evidence that two independent processes can never both reach external I/O.
- Replay does not bind the tool-handler/source version. The runtime reuses completed outcomes after restart but does not freeze a source fingerprint or prove that a changed tool implementation is semantically equivalent. Its explicit divergence guard checks a recorded tool name against the replayed decision; it is weaker than Chidori's strict source/argument replay verifier.
- Actor/evidence on manual outcome resolution are structurally required but not cryptographically authenticated or independently verified. A free-form actor string and evidence dictionary are an audit hook, not proof that the claimed remote outcome is true.
- The FastAPI control plane lacks authentication, RBAC and a production approval UI; provider integrations are application-supplied. The committed tests use deterministic fake/in-process tools, not live payment/email/provider APIs.
- The test suite was inspected, not independently rerun in this shadow run.

INDEPENDENT RED-TEAM / VERIFIER: PASS_WITH_LIMITS. The strongest commodity objection is that ambiguity-aware durable execution already exists outside this repository: mature workflow engines and at least one independent durable-tool runtime document the same principle that non-idempotent writes with unknown outcomes must become indeterminate/HITL rather than automatic retries. Therefore the value is not a novel distributed-systems theorem. The useful artifact is the compact, readable, agent-specific implementation plus regression tests that make the unsafe crash window concrete and keep approval authority outside the model. The strongest technical objection is concurrency: without a run lease or transaction spanning ownership plus effect dispatch, the repository must not be advertised as multi-worker exactly-once protection. The strongest evidence objection is provenance drift: completed effects are replayed without source fingerprints, so this is weaker as a deterministic verification fixture than Chidori. These limits lower rarity/operability but do not contradict the narrower claim that the repository implements and tests fail-closed ambiguous-outcome recovery in a single-journal agent runtime.

COMMERCIAL WEDGE: Use the semantics as a **High-Risk Agent Action Safety Adapter** rather than selling this repository as a complete orchestration platform. For one workflow that can issue refunds, credits, billing corrections, account changes, messages or other non-idempotent actions, add persist-before-act call IDs, explicit idempotency class, approval gates, completed-outcome replay and an `INDETERMINATE -> evidence-backed resolution` state. The paid deliverable is an incident-oriented safety retrofit plus crash-window tests. The economic path is avoided duplicate refunds/credits/actions and faster operator recovery from timeout/acknowledgement ambiguity. Production deployment would still require a stronger ownership/lease layer, authenticated operator identity and provider-specific status lookup.

SECOND DEEP CANDIDATE / COMPARATOR: `davccavalcante/agenticstash@b73efb4378df37de893901b9016abf6498f2474f` independently confirms that deterministic agent record/replay is becoming a reusable primitive rather than only a framework feature. Its replay engine is strict by default on input hashes, raises on exhausted recordings, reports missing/extra calls, provides fork/diff facilities, and has a tested SHA-256 tamper-evident seal. The tree contains 14 test suites and the initial-release commit claims 86 tests, zero runtime dependencies and SLSA release provenance. It is stronger than `agent-runtime` as a framework-neutral replay/evidence library but does not supply crash-resume, approval or ambiguous external-write recovery. Its `onDivergence: collect` mode can execute an extra call live when a producer is supplied while still marking the report mismatched, so it should not be confused with a universally no-live verifier. Apache-2.0.

ECOSYSTEM COMPARATOR: Phrony's public durability documentation independently uses persist-before-act invocation records, explicit side-effect classes and an `indeterminate -> HITL` path for `non_idempotent_write` / `irreversible_action` after uncertain outcomes. That convergence reduces novelty credit for the pattern while increasing confidence that **ambiguity-aware side-effect recovery** is a real emerging runtime requirement rather than an idiosyncratic design choice.

VALUE HANDOFF:
1. CAPABILITY DELTA: the production architecture gains a precise failure-state primitive missing from ordinary replay: `STARTED + NO AUTHORITATIVE OUTCOME + NON_IDEMPOTENT => INDETERMINATE/HOLD`, never “retry because no success row exists.”
2. GRAPH EDGE: directly strengthens governance/approval, restart recovery and external-action evidence boundaries. It complements Chidori: Chidori is stronger on source-bound strict replay; `agent-runtime` is clearer on the external commit/acknowledgement ambiguity window.
3. RADAR SIGNAL: independent implementations now converge on two layers of agent reliability: deterministic replay for known past effects, and explicit indeterminate-state handling for effects whose remote outcome cannot be inferred from local persistence.
4. EXPERIMENT IMPACT: kill/restart tests should add a crash point immediately after external dispatch but before local success persistence. Passing requires no automatic replay for non-idempotent actions and a separate evidence-resolution path.
5. COMMERCIAL IMPACT: the most defensible buyer is an AI/application platform team exposing money-mutating or customer-visible tools. The first wedge is an action-safety retrofit, not framework migration. ROI can be measured in duplicate-action incidents prevented, ambiguous-outcome MTTR and percentage of high-risk tools covered by provider-side reconciliation.
6. NEGATIVE KNOWLEDGE: journal append uniqueness is not a run lease, “idempotent” must be a domain/provider claim rather than a casual developer label, and operator-entered resolution metadata is not itself authoritative evidence. Future candidates need an explicit source of truth for resolving ambiguity.

EFFORT: 5 materially distinct discovery/validation modes; 2 deep inspections (`prathamesh-git9/agent-runtime`, `davccavalcante/agenticstash`); 1 external ecosystem comparator; approximately 18 external research calls; 0 reproducible test executions in this run.

## Run 3 — 2026-09-20

HYPOTHESIS: The next reliability step is not another checkpoint primitive but a **fenced external-effect boundary**: a production-shaped agent runtime should durably derive effect identity, fence multi-worker ownership before dispatch, make terminal state atomic with durable evidence, and force ambiguous post-dispatch outcomes into an explicit reconciliation state. Ideally that same runtime would also bind replay to source/version provenance and resolve external ambiguity against provider/system-of-record status. If strong candidates implement only the first four pieces, the correct architecture is compositional rather than waiting for one monolithic agent framework.

DISCOVERY METHODS: (1) direct search for durable agent runtimes combining replay/idempotency with leases, fencing and tool effects; (2) code-level search around `LeaseFence`, effect outboxes, owner generations, `TOOL_EXECUTION_STARTED`, uncertain delivery, claim TTLs, `SKIP LOCKED`, retry keys and reconciliation; (3) deliberate low-attention search for zero/low-star runtime repositories with production-shaped PostgreSQL effect ledgers; (4) ecosystem comparison against an Elixir/Jido durable-workflow runtime with claim tokens/fingerprints; (5) commit/history archaeology on the effect-outbox implementation and deployment pins. Specialist decomposition: CODE INSPECTOR traced effect identity, transaction boundaries, terminal writes and test fault seams; RELIABILITY ANALYST evaluated worker ownership, stale-fence rejection, crash ambiguity and retry admissibility; COMMERCIAL ANALYST scoped a generic reliability-hardening wedge; independent RED-TEAM/VERIFIER received the frozen claim packet without the proposed score and tried to disprove exactly-once, provenance and provider-reconciliation claims.

BEST CANDIDATE: `teleport-computer/feedling-mcp`
CANONICAL URL: https://github.com/teleport-computer/feedling-mcp
EXACT REVISION: `c37e3db0dcc4f08cf69aa3c73cf87a911377b02f`
EVIDENCE SNAPSHOT: `shadow-ai-20260920-feedling-c37e3db`
VERIFIER VERDICT: PASS_WITH_LIMITS
PROPOSED SCORE: A3 B4 C5 D5 E5 F4 = 26/30. This is a shadow proposal only, not a MASTER promotion.

CLAIM / EVIDENCE PACKET:
- IMPLEMENTED + TESTED: Runtime V2 uses a generation/owner-fenced durable effect outbox. `backend/model_api_runtime/v2/effect_outbox.py` derives deterministic effect IDs, performs retry-idempotent enqueue, and has an owner-fenced path that locks and rechecks the source job in the same transaction as the effect insert so recovery cannot observe “no durable effect yet” while a current owner is still publishing.
- IMPLEMENTED + TESTED: `tests/test_v2_p0_exactly_once.py` explicitly fault-injects crashes before write, after sink write but before the outbox status flip, after status, and during the write. Across crash and recovery the sink write count remains exactly one for the tested durable sink contract.
- IMPLEMENTED + TESTED: the load-bearing hard-crash case models `claim committed -> process dies before target-write outcome is knowable`. Recovery does not guess or retry. It raises `EffectDeliveryUncertainError`, performs no second durable write, moves the outbox row to `needs_reconciliation`, and persists a sanitized `delivery_uncertain` reason while the sink claim remains incomplete.
- IMPLEMENTED + TESTED: a separate production-sink regression proves the opposite branch: when the underlying write itself raises before anything durable lands, the sink claim is released, the outbox remains pending, and a later retry performs one successful durable write. This distinguishes **proved no effect** from **unknown effect**, which is the key ambiguity invariant.
- IMPLEMENTED: outbox application is fenced by runtime state/generation and per-job ownership. The code rejects stale owners rather than silently no-oping platform writes; each pending row is processed under a transaction that locks runtime authority before apply.
- IMPLEMENTED + DEPLOYMENT-SHAPED: the inspected head is a deployment-pin commit that points production Runtime V2 images at parent revision `9b08137`; repository metadata shows an MIT license and low attention (9 stars at inspection time), while the deployment files include separate agent-runner and serve-worker processes.

CLAIMS NOT VERIFIED / LIMITS:
- Feedling's strongest exactly-once proof is **within its sink-ledger contract**. For a generic third-party API that can commit remotely after the local sink claim but before local completion, the runtime still needs provider-specific status/idempotency evidence or human reconciliation. The local claim ledger cannot prove remote state by itself.
- I did not find Chidori-style source fingerprints that bind a completed tool/effect result to the exact tool implementation and enforce a strict no-live replay fixture. Feedling has runtime generation/model-route metadata and strong effect identity, but that is not equivalent to source-bound deterministic replay verification.
- `needs_reconciliation` is the correct fail-closed state, but this inspection did not establish an authenticated, provider-backed generic resolver that automatically turns every ambiguous external effect into authoritative succeeded/failed truth.
- The committed PostgreSQL tests were inspected rather than independently rerun here.
- Feedling is a full product/runtime, not a small reusable library. Extracting its effect semantics into another stack may be significantly more work than adopting a purpose-built workflow engine or implementing the small state machine directly.

INDEPENDENT RED-TEAM / VERIFIER: PASS_WITH_LIMITS. The strongest objection is that “exactly once” can be misleading if generalized beyond the tested sink boundary. The repository earns credit because its tests explicitly separate write-failed from write-possibly-committed and because the latter goes to `needs_reconciliation` rather than being retried. It also has materially stronger multi-worker authority semantics than Run 2's single-journal candidate. However, the all-four hypothesis is only partially satisfied: effect identity + owner fencing + ambiguity state + durable tests coexist, while source-bound strict replay and universal provider-side reconciliation do not. Therefore the candidate is a strong **reference implementation for external-effect correctness**, not proof of end-to-end exactly-once against arbitrary third-party services.

COMMERCIAL WEDGE: Package the pattern as an **Agent External-Action Reliability Retrofit** for teams whose agents issue customer-visible or money-mutating actions. Deliver deterministic effect IDs, a fenced outbox, per-sink claim ledger, crash-window fault tests, `FAILED_NO_EFFECT` versus `NEEDS_RECONCILIATION` semantics, and provider-specific readback adapters where available. The value is fewer duplicate actions, fewer silent lost actions, faster incident resolution, and clearer audit evidence. The first paid deployment should target one high-risk action family rather than replacing the customer's orchestration stack.

SECOND DEEP CANDIDATE / COMPARATOR: `LogicStormINC/zebra@efd4e2938d14c6598e9c60503830abf5360fa0bf` is a zero-star agent platform with an unusually explicit PostgreSQL `EffectDispatch` aggregate. Its source uses `LeaseFence(control_plane_epoch, fencing_token, owner_instance_id)`, `FOR UPDATE SKIP LOCKED`, atomic terminal event/result writes, `UNCERTAIN`, `FAILED_NO_EFFECT`, evidence history and retry keys. Real-PostgreSQL tests cover concurrent schedule idempotency, stale-fence rejection, one-consumer claims, terminal rollback, epoch replacement, concurrent reconciliation CAS and “expired claim never returns to pending.” It is a strong architectural comparator and independently confirms the same fail-closed effect state machine. The main caveat is that its `EffectEvidence.provider_operation_id_hash` is evidence plumbing rather than a built-in authoritative provider-status integration; public license metadata is absent.

THIRD COMPARATOR: `agentjido/jizoku@aeb1871c2d297244b77aa718722e3dd79cac4504` independently confirms durable workflow convergence on internal claim tokens, heartbeats, lease-loss termination, effect outboxes and exact workflow-definition fingerprint fencing. It is broader and more established than the zero-star candidates, but this run did not deep-verify an equivalent third-party-provider ambiguity resolver, so it remains ecosystem evidence rather than the best candidate.

VALUE HANDOFF:
1. CAPABILITY DELTA: the architecture now has a production-shaped pattern for **fenced effect dispatch**: deterministic effect identity + owner/generation fence + sink claim + atomic terminal fact + explicit uncertain state + retry only after proof that no effect occurred.
2. GRAPH EDGE: directly strengthens the production architecture's lease-recovery and verifier obligations, and closes Run 2's biggest missing piece: multi-worker ownership before external dispatch. It still needs Chidori-style source provenance and domain/provider reconciliation to close the full end-to-end loop.
3. RADAR SIGNAL: three independent runtime families now converge on effect outboxes, fencing/claim tokens and explicit uncertainty rather than “retry on timeout.” This looks like a genuine emerging reliability layer for long-horizon agents, not just framework-specific style.
4. EXPERIMENT IMPACT: acceptance tests should distinguish at least four crash points: before sink claim, after claim/before remote write, after remote write/before local terminal fact, and after terminal fact/before response. The expected recovery action must differ by what can be proven, not merely by exception type.
5. COMMERCIAL IMPACT: a reusable reliability retrofit becomes more concrete. The smallest sellable unit is one fenced high-risk action family with crash-window tests and provider readback, not a wholesale agent-runtime migration.
6. NEGATIVE KNOWLEDGE: local exactly-once ledgers do not create remote exactly-once semantics. A candidate must identify the external authority that can resolve uncertain operations; source/version provenance is a separate axis from worker fencing and must not be inferred from generation IDs.

EFFORT: 5 materially distinct discovery/validation modes; 3 serious candidates inspected (`teleport-computer/feedling-mcp`, `LogicStormINC/zebra`, `agentjido/jizoku`); approximately 24 external research calls; 0 reproducible test executions. A final broader GitHub search for provider-specific reconciliation hit a transient code-search rate limit and was not treated as evidence.

## Run 4 — 2026-09-20

HYPOTHESIS: The most valuable missing edge from Runs 1–3 is a **real provider-backed reconciliation boundary** that unifies exact action identity, durable local state, provider idempotency, read-only provider truth, crash-window qualification, and source/configuration provenance. A candidate that closes those edges should be more valuable than another generic replay/checkpoint runtime even if production rollout is deliberately blocked until real-provider qualification succeeds.

DISCOVERY METHODS: (1) direct search for agent/payment runtimes using provider idempotency and recovery; (2) code-level search around `Idempotency-Key`, `outcome-unknown`, `reconcile`, provider truth, refund retrieval/listing, reservation state machines and exact command commitments; (3) provider-specific search for Stripe refund ambiguity/readback implementations; (4) commit-history archaeology around qualification, semantic closure and recovery hardening; (5) comparator inspection of a Temporal-based real-Stripe refund agent and a broader durable-effects framework. Specialist decomposition: CODE INSPECTOR traced Stripe command construction, reservation states and read-only recovery; RELIABILITY ANALYST traced crash boundaries and provider-truth requirements; GOVERNANCE/VERIFICATION pass inspected source/configuration closure and route qualification; COMMERCIAL ANALYST scoped a high-risk-action safety service; independent RED-TEAM/VERIFIER reviewed the frozen evidence without the proposed score and tried to disprove live-provider proof, exactly-once scope, audit maturity and deployability.

BEST CANDIDATE: `auths-dev/auths-proof`
CANONICAL URL: https://github.com/auths-dev/auths-proof
EXACT REVISION: `34fa1f33cf365fa54075a2002710aee52ab42394`
EVIDENCE SNAPSHOT: `shadow-ai-20260920-auths-proof-34fa1f33`
VERIFIER VERDICT: PASS_WITH_LIMITS
PROPOSED SCORE: A3 B5 C5 D5 E4 F3 = 25/30. This is a shadow proposal only, not a MASTER promotion.

CLAIM / EVIDENCE PACKET:
- IMPLEMENTED: the Stripe vertical is an exact-action adapter rather than a generic proxy. The protected path binds account, API version, PaymentIntent/Charge identity, integer minor-unit amount, currency, reason, configuration/evidence commitments and a deterministic provider idempotency key before constructing the outbound request. Source inspection shows the provider mutation sends Stripe's `Idempotency-Key` and the recovery path uses a separate read-oriented reconciliation function rather than silently minting a new action.
- IMPLEMENTED: `product/integrations/auths-stripe/src/reservation.rs` models the durable financial state explicitly as `Reserved`, `Committed`, `Released`, `OutcomeUnknown`, `ReconciledCommitted`, and `ReconciledReleased`. The record commits workflow/action identity, policy/evaluator identity, evidence/configuration digests, account/currency/amount, aggregate-budget intents, provider-idempotency commitment and provider-result identity. `release` is documented only for definite non-execution; ambiguous delivery is held and `reconcile` resolves it from provider evidence.
- IMPLEMENTED: `product/integrations/auths-stripe/src/qualification.rs` exposes distinct execute and reconcile transports. Its reconcile seam is documented as observing the original idempotent refund **without submitting another mutation**, while `observe_provider_truth` uses a protected read credential and emits a closed redacted fact set. The reviewed provider contract separates mutation (`refunds.write`) from runtime read permissions (`charges.read`, `payment-intents.read`, `refunds.read`).
- IMPLEMENTED + QUALIFICATION-HARNESS EVIDENCE: `qualification/failpoint-coverage-v1.json` enumerates durable/provider crash boundaries. The load-bearing `crash-after-request-write` case permits either provider-applied or not-applied truth but requires `no-second-provider-call`, `provider-request-write-once`, a stable operation ID, and provider truth before recovery. Later provider-result/observation/receipt crash points likewise require no second provider call.
- IMPLEMENTED: source/configuration provenance is unusually explicit. The live-provider qualification contract defines a semantic closure over shipping source, manifests, schemas, canonical fixtures, generated route inputs and qualification workflow code; route qualification is supposed to become invalid when that closure drifts. Bounded refund configuration also commits evaluator implementation/build ID, policy digest, exact-action profile, reservation/receipt schemas and executed-vs-required configuration.
- TESTED NEGATIVE CONTROL: `product/runtime/auths-node/tests/profile_qualification.rs` asserts that a normal production build advertises **no unqualified provider profiles**. A qualification-only build exposes the exact candidate roster without qualification claims, and the disposable testkit exposes only its isolated Stripe route.
- HISTORY: recent commit history contains repeated formal/qualification artifact regeneration with source-SHA/workflow-run provenance and a dedicated commit `ci: decompose and accelerate formal qualification`, plus earlier qualification semantic-freeze work. This is active implementation work rather than an abandoned design document.
- PROVENANCE: workspace license is `MIT OR Apache-2.0`; version metadata at the inspected head is `1.0.0-rc.1`.

CLAIMS NOT VERIFIED / LIMITS:
- **The critical limitation is explicit and machine-visible: this revision is not live-provider qualified.** `release/qualification/v1/index.json` contains `entries: []`, the production-profile test asserts the production route set is empty, and the live-qualification specification says Stripe/PostgreSQL/OpenTofu begin unqualified. Therefore this run does NOT claim that real Stripe crash recovery has already passed the protected qualification matrix.
- The repository is prelaunch and pre-audit. Strong internal specifications, formal tooling and committed harnesses are not substitutes for an external security review or imported live-provider attestation.
- The source tree is very large. I verified the Stripe recovery/state/qualification seams and their gating tests, not every proof, cryptographic primitive, provider domain or generated artifact.
- A local clone/test execution attempt was blocked by the execution sandbox's lack of outbound DNS. No repository tests were independently rerun in this shadow run.
- The architecture is domain-specific and opinionated. Integrating exact proof-carrying authority, credential isolation and provider qualification into an existing agent stack may cost materially more than adopting only the small `OUTCOME_UNKNOWN -> provider readback` state machine.

INDEPENDENT RED-TEAM / VERIFIER: PASS_WITH_LIMITS. The candidate survives the narrow claim that it implements a unusually complete **architecture** for exact authority + durable reservation + provider idempotency + read-only provider reconciliation + source/configuration qualification. It fails any stronger claim that these guarantees are already production-proven against Stripe: the checked qualification index is empty and production routes intentionally remain unavailable. That self-denial is actually positive evidence of fail-closed governance, but it lowers operational readiness. The commodity objection is that Temporal plus Stripe idempotency can already prevent many duplicate refunds; the non-commodity increment here is the combination of exact action authorization, durable aggregate reservations, explicit unknown outcomes, provider-truth recovery, credential separation, redacted receipts, semantic source closure and a machine gate that refuses to advertise the route until real-provider crash evidence is imported. The strongest residual risk is complexity: a team may obtain most economic benefit with a smaller provider-aware outbox and independent policy layer.

COMMERCIAL WEDGE: **High-Risk Agent Action Qualification**. Wrap one money-mutating/customer-visible action family—refund, credit, payout, procurement mutation, account change—behind an exact-action contract, durable reservation/claim, provider idempotency, provider readback, evidence receipts and a crash matrix. Sell the first engagement as a bounded safety qualification and integration, not as a framework rewrite. The measurable outputs are duplicate-action prevention, explicit ambiguous-outcome MTTR, percentage of provider crash windows covered, and proof that denied/unqualified actions never acquire mutation credentials. Auths itself should remain a reference/possible substrate until live qualification exists for the chosen production adapter.

SECOND DEEP CANDIDATE / COMPARATOR: `temporal-community/agent-memory-and-state@59fb4186bc50daefe09653a850197a985b3fa1cf` is an MIT-licensed Temporal refund-agent demo with a deterministic workflow loop, durable human approval/waits, a real `stripe.Refund.create` path, and a stable idempotency key derived from Temporal workflow/run identity. Activity retries reuse that key after worker loss. This is strong evidence that durable orchestration + provider idempotency is already accessible with mainstream workflow infrastructure. However the inspected effect path primarily retries the same provider request; this run did not find a separate provider-truth reconciliation state machine or source-closure qualification gate comparable to Auths. Its pedagogical commit history explicitly includes a simulated Stripe timeout/retry demo, so it is a useful commodity baseline rather than the best match for the full hypothesis.

NEGATIVE / NEAR-MISS: `pipeshub-ai/loom@8d630343cad625e2509d8790d98f9ed0afa13bfa` has extensive durable-step/journaling work and high test volume, but its own browser-effect design notes that replay can re-perform effects and requires the flow author to supply an idempotency key or compensation for submissions that must not repeat. That is useful honesty, but it does not close the provider-truth ambiguity edge this run targeted.

VALUE HANDOFF:
1. CAPABILITY DELTA: the portfolio gains a concrete **provider-backed reconciliation blueprint**: exact action + local reservation/claim + deterministic provider idempotency + explicit unknown state + read-only provider truth + crash qualification + source/configuration closure.
2. GRAPH EDGE: this closes the biggest missing edge from Runs 1–3 at the architecture level. Chidori still leads strict deterministic replay; Feedling/Zebra lead compact fenced-effect semantics; Auths adds provider-specific recovery and a governance/evidence gate around when the adapter may be advertised.
3. RADAR SIGNAL: agent reliability appears to be converging from “durable workflow” toward **qualified action adapters** whose guarantees are domain/provider-specific, source-bound and evidence-backed. This is more likely to matter for money/compliance actions than a universal exactly-once slogan.
4. EXPERIMENT IMPACT: future acceptance tests should not stop at injected crashes. They should require independent provider truth for each crash window and prove that source/config drift invalidates qualification. A green local journal alone is insufficient.
5. COMMERCIAL IMPACT: the sellable unit becomes clearer: one high-risk action family can be qualified end-to-end without replacing the customer's whole agent framework. The service value is evidence that the action cannot silently duplicate, exceed policy, or self-authorize across the tested crash/provider boundary.
6. NEGATIVE KNOWLEDGE: a sophisticated qualification harness is not the same thing as a qualified production adapter. Always inspect the trusted qualification index/attestation and the runtime advertisement test before crediting live-provider guarantees.

EFFORT: 5 materially distinct discovery/validation modes; 2 deep inspections (`auths-dev/auths-proof`, `temporal-community/agent-memory-and-state`) plus one near-miss architecture review (`pipeshub-ai/loom`); approximately 30 external research calls; 0 reproducible repository test executions, with 1 local clone attempt blocked by sandbox DNS.

## Run 5 — 2026-09-20

HYPOTHESIS: A stronger long-horizon agent runtime should treat **model submission ambiguity** and **external-action ambiguity** as separate durable failure states, and where possible resolve the external-action side against authoritative provider state rather than blindly reissuing the mutation. A serious candidate should also fence ownership, detect tool/configuration drift, and have crash/recovery evidence beyond README claims.

DISCOVERY METHODS: (1) direct search for ambiguous external-effect/provider-reconciliation agent runtimes; (2) code-level search around `SubmittedUnknown`, `OutcomeUnknown`, external effects, durable claims/fences, publications and replay/recovery; (3) provider-side recovery search for GitHub publication/PR reconciliation paths; (4) commit-history archaeology around external-effect invariants and ambiguous publication recovery; (5) comparator search against application-level refund/idempotency implementations. Specialist decomposition: CODE INSPECTOR traced the model-dispatch and external-effect state machines plus PR recovery; RELIABILITY/GOVERNANCE ANALYST inspected ownership, ambiguity, tool drift and live CI evidence; COMMERCIAL ANALYST mapped the semantics to coding/deployment and eventually money-mutating agents; independent RED-TEAM/VERIFIER tried to reduce the result to ordinary retry/idempotency behavior and to identify any unproven exactly-once or provider-qualification claim.

BEST CANDIDATE: `orka-agents/orka`
CANONICAL URL: https://github.com/orka-agents/orka
EXACT REVISION: `7cffe130efa74f505d66dce561ade6c18901ba6e`
EVIDENCE SNAPSHOT: `shadow-ai-20260920-orka-7cffe130`
VERIFIER VERDICT: PASS_WITH_LIMITS
PROPOSED SCORE: A3 B5 C5 D5 E5 F4 = 27/30. This is a shadow proposal only, not a MASTER promotion.

CLAIM / EVIDENCE PACKET:
- IMPLEMENTED: `api/v1alpha1/external_effect_types.go` models external effects as durable first-class state with explicit phases including `Preparing`, `Dispatching`, `Dispatched`, `Succeeded`, `Failed`, and `OutcomeUnknown`. Ambiguous remote outcomes are not silently collapsed into success or failure.
- IMPLEMENTED + TESTED: `internal/controller/acp_external_effect.go` drives the effect state machine and turns expired/in-flight ambiguity into `OutcomeUnknown` rather than assuming no remote effect occurred. Retryable failures are distinct from ambiguous outcomes; safe retry still depends on stable operation identity plus adapter/provider idempotency semantics.
- IMPLEMENTED: `internal/controller/harness_v1_dispatcher.go` independently models **model-call submission ambiguity** through a durable `SubmittedUnknown` state. The runtime therefore recognizes that “request may have left this process” is a reliability boundary even before a tool or external action is involved.
- IMPLEMENTED + TESTED: `internal/controller/acp_publication_recovery.go` and `acp_publication_pr_recovery_test.go` implement a concrete provider-readback pattern for GitHub PR publication. Recovery searches provider-side state and reconciles an ambiguous publication from the observed remote object rather than blindly issuing another create. Committed tests cover existing-PR recovery and duplicate-avoidance behavior.
- IMPLEMENTED + TESTED: `internal/controller/acp_mcp_broker_tool_drift_test.go` exercises tool/broker drift and demonstrates that changed tool/configuration assumptions can block unsafe continuation instead of inheriting stale execution assumptions. This is strong drift-fencing evidence, though it is not equivalent to a cryptographically signed source-binary qualification artifact.
- HISTORY: commit archaeology shows the external-effect/publication area was repeatedly hardened rather than added once and forgotten. Follow-up changes tightened terminal-outcome invariants, publication outcome identity, PR reconciliation, ambiguous recovery with corrupted URLs/provider lookup failures, collision handling and scope-error preservation.
- LIVE OPERATIONAL EVIDENCE: GitHub Actions for the pinned head included a successful `Live GitHub OIDC PR E2E` workflow on 2026-09-20. This is stronger than README-only provider claims and demonstrates a real GitHub publication path at this revision, but it is still CI evidence rather than a signed qualification corpus.

CLAIMS NOT VERIFIED / LIMITS:
- The strongest provider-backed recovery inspected here is GitHub PR publication. This run did not prove equivalent reconciliation semantics for Stripe, refunds, payouts or arbitrary third-party money APIs.
- `OutcomeUnknown` is a correct fail-closed state, not a source of truth. If an adapter lacks provider readback/idempotency, the runtime still cannot infer whether a remote mutation committed.
- Transport-level retry remains safe only to the extent that stable operation identity and the adapter/provider actually provide idempotent semantics. Kubernetes/CRD durability does not create remote exactly-once behavior by itself.
- The MCP/tool drift evidence is strong operational drift protection but is not a signed semantic-closure attestation like the more ambitious Auths design.
- I did not find a public non-empty trusted provider-attestation index in this run. The successful live GitHub E2E must not be relabeled as such an attestation.
- The committed tests and workflow evidence were inspected, not independently rerun locally in this automation turn.

INDEPENDENT RED-TEAM / VERIFIER: PASS_WITH_LIMITS. Orka survives a materially narrower but valuable thesis: it combines two explicit uncertainty boundaries—model submission and external-effect dispatch—with durable external-effect state, concrete provider-side GitHub PR reconciliation, drift-oriented tests, and recent live-provider CI evidence. The strongest objection is scope: the provider reconciliation is specialized, and the repository does not prove universal exactly-once behavior or a signed live-provider qualification system. The commodity objection is that durable workflow engines plus provider idempotency can solve large parts of this problem; Orka's differentiated value is the integrated agent-specific treatment of submission ambiguity, external-effect ambiguity and provider-aware publication recovery. Those caveats prevent an unqualified PASS but do not negate the implemented reliability pattern.

COMMERCIAL WEDGE: **High-Risk Agent Action Qualification for coding/ops agents**. Start with one GitHub PR or deployment mutation: require durable action identity, explicit unknown states, ownership/fencing, tool-drift rejection and provider readback before retry/resolution. Deliver the crash/recovery evidence as the product. The same contract can later be adapted to one financial mutation, but that second step should require a provider-specific authoritative status/readback path rather than assuming the GitHub pattern transfers automatically.

COMPARATOR / NEAR-MISS: `BELCORT-SDN-BHD/FIKIRTIVE@18a36c49daf431b7573d86cd310b195bcc3fa001` contains application-level refund/idempotency/manual-pending behavior, but it does not expose a comparable general runtime with first-class model/effect ambiguity, durable effect state and provider-reconciliation tests. It is useful as evidence that product teams hand-build fragments of this pattern, not as a stronger architecture candidate.

VALUE HANDOFF:
1. CAPABILITY DELTA: adds a **dual-uncertainty runtime pattern**: model submission can become `SubmittedUnknown`, external mutations can become `OutcomeUnknown`, and selected mutations can be reconciled against provider truth rather than reissued.
2. GRAPH EDGE: strengthens the previously missing edge from “ambiguous remote action” to “authoritative readback” with a concrete GitHub publication implementation, while also adding a separate model-provider ambiguity edge absent from the earlier candidates.
3. RADAR SIGNAL: independent agent runtimes are converging on explicit uncertainty states, effect identity, fenced ownership and provider readback. The emerging unit is not “exactly once”; it is **evidence-backed ambiguity resolution per external action family**.
4. EXPERIMENT IMPACT: a practical first qualification experiment can use GitHub PR publication because Orka already supplies the ambiguous-publication/recovery seam and live E2E path. Acceptance should then mutate tool/configuration identity and verify that stale assumptions fail closed.
5. COMMERCIAL IMPACT: enterprise coding/ops agents become a nearer-term buyer than money APIs for this exact implementation. A bounded PR/deployment-action qualification can demonstrate avoided duplicate mutations and faster ambiguous-outcome recovery before adapting the architecture to finance.
6. NEGATIVE KNOWLEDGE: live CI success is not a trusted qualification artifact; provider reconciliation is action-specific; durable UNKNOWN states are only valuable when the system can later obtain authoritative evidence or deliberately require human resolution.

EFFORT: 5 materially different discovery/validation modes; 2 serious implementation paths inspected (`orka-agents/orka` and the FIKIRTIVE comparator); source, tests, schema/state definitions, commit history and live CI were checked; 0 fresh local test executions. A broader public search for non-empty provider attestations produced no verified candidate and later hit transient GitHub code-search rate limits.

NEXT HIGHEST-VALUE QUESTION: Can we find a public implementation with a **non-empty, independently verifiable live-provider qualification artifact** where source/configuration drift provably withdraws a production route for a money-mutating action?

## Run 6 — 2026-09-20

HYPOTHESIS: A framework-neutral financial-action runtime is materially more valuable than a generic durable-agent runtime when it binds approval to the exact proposed effect, rechecks live business state/authority, gives ambiguous provider outcomes an explicit non-retry recovery path, and emits provider-qualification evidence tied to exact source/provider versions. However, a qualification harness without a retained successful provider report must remain **unqualified**, even if the source and tests are otherwise strong.

DISCOVERY METHODS: (1) direct search for provider-qualified financial/agent action runtimes using refunds, idempotency and authoritative reconciliation; (2) code-level search around `failed_unknown`, `authoritative query`, qualification reports, Stripe sandbox evidence, semantic-effect claims and recovery; (3) deliberate low-attention search emphasizing zero-star financial action runtimes and adjacent merchant-agent implementations; (4) workflow/history archaeology for protected provider-qualification jobs, report schemas and corrective release history. Specialist decomposition: CODE INSPECTOR traced lifecycle states, database transition constraints, semantic-effect admission and Stripe recovery; PROVIDER/VERIFICATION ANALYST inspected the sandbox qualification workflow/report schema and distinguished exercised versus not-exercised evidence; COMMERCIAL ANALYST scoped a bounded financial-action safety retrofit; independent RED-TEAM/VERIFIER reviewed the frozen claim packet without the proposed score and attempted to disprove provider qualification, exactly-once scope, host-independence and operational maturity.

BEST CANDIDATE: `BlackPigIndustries/threvo-actions`
CANONICAL URL: https://github.com/BlackPigIndustries/threvo-actions
EXACT REVISION: `f50d568e8d67d1806a122b3151048f038832dcf9`
EVIDENCE SNAPSHOT: `shadow-ai-20260920-threvo-actions-f50d568e`
VERIFIER VERDICT: PASS_WITH_LIMITS
PROPOSED SCORE: A4 B5 C5 D4 E4 F4 = 26/30. This is a shadow proposal only, not a MASTER promotion.

CLAIM / EVIDENCE PACKET:
- IMPLEMENTED: the supported 0.6.2 runtime is explicitly a control lifecycle around consequential actions, not a payment SDK. It binds authority to the exact proposal/effect, re-resolves current business state before execution, uses tenant-scoped compare-and-set plus semantic-effect admission, treats transport acceptance as non-terminal, and allows only authoritative verification to reach `verified`.
- IMPLEMENTED: `src/threvo_actions/runtime.py` exposes distinct durable outcomes including `VERIFICATION_PENDING`, `FAILED_KNOWN`, `FAILED_UNKNOWN`, `VERIFICATION_UNRESOLVED` and `RESEND_ALLOWED`. The runtime describes itself as coordinating host-owned controls without owning host business truth, which correctly separates orchestration from source-of-truth authority.
- IMPLEMENTED + SCHEMA-ENFORCED: the PostgreSQL migration protects proposal identity/revision and restricts lifecycle transitions. An executing proposal may become `failed_unknown` or `verification_pending`; a `failed_unknown` proposal may only advance to `verification_pending`; and the effect claim transfer helper permits replacement only when the current owner is `failed_known` or `stale`, not merely unknown. That prevents the most dangerous “timeout means safe to retry” interpretation at the database boundary.
- IMPLEMENTED + TESTED HARNESS: the Stripe sandbox qualification suite contains a real lost-acknowledgement scenario. `_LoseAcceptedResponse.create()` increments a submission counter, sends the real Stripe sandbox mutation, then intentionally discards the successful response by raising `StripeBoundaryError`. The test then runs recovery, requires final lifecycle `VERIFIED`, validates the exported evidence bundle, and asserts `loss_gateway.submissions == 1`.
- IMPLEMENTED QUALIFICATION EVIDENCE MODEL: `StripeQualificationReport` distinguishes `PASSED`, `FAILED` and `NOT_EXERCISED`; records scenario/action group/disposition/evidence class, exact `source_revision`, Stripe SDK/API versions, account mode, database/provider participation and cleanup status; and refuses non-test Stripe credentials. Missing sandbox inputs produce a `NOT_EXERCISED` report rather than a synthetic pass.
- IMPLEMENTED WORKFLOW: `.github/workflows/stripe-qualification.yml` is a manual protected-environment workflow with a disposable PostgreSQL service, dedicated sandbox credential, branch guard, pinned actions, explicit Stripe qualification marker, and artifact upload named by exact source SHA. The workflow exists as a credible evidence generator rather than proof by itself.
- HISTORY / MATURITY: the exact head is a signed merge for a corrective Stripe subscription-cancellation eligibility fix. The 0.6.2 release record states that provider eligibility was corrected without weakening host authorization/live preconditions, and it explicitly says the maintainer-owned Threvo pilot does **not** establish independent adoption or production qualification.
- PROVENANCE: Apache-2.0. The repository was zero-star/zero-fork at inspection time despite a release-shaped source tree, database adapters, conformance contracts and a financial-provider qualification harness.

CLAIMS NOT VERIFIED / LIMITS:
- **The live Stripe qualification target remains unmet at this revision.** `docs/testing/stripe-sandbox-qualification.md` says that as of 12 September 2026 the required sandbox key and database were not configured, all six collected cases were `not_exercised`, no Stripe resource was changed, and provider qualification remained pending. Therefore this run does NOT claim a retained successful provider report exists for the pinned revision.
- A protected manual workflow and an executable real-sandbox test do not prove that the workflow ran. The repository itself states that a provider-qualified claim requires retained successful reports for the exact source, SDK and API versions.
- The runtime intentionally depends on the host for authenticated identity, business authorization, material-drift rules, target-side idempotency, the authoritative external query, reconciliation scheduling and deployment controls. A weak host can still supply weak business truth.
- The project explicitly disclaims distributed exactly-once execution. Its semantic-effect claim prevents competing local admission; it does not guarantee a remote financial mutation occurs exactly once unless the provider supplies stable identity/readback and the host implements them correctly.
- `EventSink` is best-effort/at-most-once after persistence rather than a durable event outbox, and evidence exports are unsigned internal-consistency artifacts rather than independently non-repudiable audit proof.
- The committed source/tests/schema were inspected rather than independently rerun in this automation turn.

INDEPENDENT RED-TEAM / VERIFIER: PASS_WITH_LIMITS. The candidate survives the narrow claim that it implements an unusually coherent **financial consequential-action lifecycle**: exact authority, live re-resolution, semantic-effect admission, explicit ambiguous outcome, authoritative verification, safe-resend gating and a real Stripe qualification harness. The strongest objection is evidence status: the repository's own current-status document says the provider cases were not exercised, so no live Stripe qualification can be credited. The strongest commodity objection is that durable workflow engines plus Stripe idempotency already solve much of retry safety; Threvo's differentiated increment is the framework-neutral merchant/application control layer that also binds human/agent authority, business-state drift, local effect ownership, recovery semantics and minimized evidence. These limits prevent an unqualified PASS but do not negate the implemented safety architecture.

COMMERCIAL WEDGE: **Agentic Financial Action Safety Layer / Qualification Retrofit.** Wrap one existing refund, cancellation, credit-note or customer-balance-credit path without replacing the customer's entire agent framework. Deliver exact approval binding, live state recheck, semantic-effect admission, provider idempotency/readback mapping, lost-response crash tests, operator recovery semantics and a minimized evidence packet. The buyer is an AI/fintech application or platform team; economic value comes from avoided duplicate refunds/credits, lower ambiguous-outcome MTTR, safer delegation of consequential actions and cheaper audit/debug reconstruction.

COMPARATOR / CATEGORY SIGNAL: `auths-dev/auths-proof@34fa1f33cf365fa54075a2002710aee52ab42394` remains stronger on explicit source/configuration semantic closure and fail-closed route qualification, while Threvo is more compact/framework-neutral and release-shaped around merchant financial actions. Both independently demonstrate the same critical negative rule: **implemented qualification machinery is not provider qualification** until exact-source live evidence exists.

VALUE HANDOFF:
1. CAPABILITY DELTA: adds a reusable **consequential financial-action lifecycle** that integrates exact authority, business-state drift refusal, local effect ownership, ambiguous-outcome verification and provider qualification reporting around existing provider SDKs.
2. GRAPH EDGE: strengthens the provider-reconciliation/authority layer of the compositional safety hypothesis, but does not replace Chidori-style source-bound replay or Feedling/Zebra-style generalized worker fencing.
3. RADAR SIGNAL: independent projects are converging on a distinct category between “agent framework” and “payment SDK”: provider-aware governed action runtimes with explicit uncertainty, authority and qualification evidence.
4. EXPERIMENT IMPACT: the next decisive experiment is now very concrete: run the exact lost-response Stripe sandbox case, retain the generated `threvo.actions.stripe-qualification/v1` report/artifact for the exact source/API/SDK, then change one behavior-bearing source/config input and verify qualification is invalidated rather than inherited.
5. COMMERCIAL IMPACT: the smallest sellable unit can be one existing financial action retrofit plus evidence, avoiding full workflow migration. Success can be measured by duplicate-action rate, ambiguous-outcome MTTR, provider-reconciliation coverage and percentage of high-risk actions requiring exact approval binding.
6. NEGATIVE KNOWLEDGE: never award “provider qualified” credit merely because a repository contains credentials-gated CI, real-sandbox tests or a qualification report schema. Inspect the retained report and require `provider_exercised=true` + `status=passed` for the exact revision; `not_exercised` is a correct fail-closed result, not partial proof.

EFFORT: 4 materially distinct discovery/validation modes; 2 serious candidates/comparators inspected (`BlackPigIndustries/threvo-actions` plus the previously validated Auths pattern) and one zero-star merchant-agent near-miss reviewed at spec level; source, committed test, database schema, release history, workflow and qualification-report schema were checked; 0 fresh local test executions.

NEXT HIGHEST-VALUE QUESTION: Can we find or independently produce a **retained successful live-provider qualification artifact** for an exact source revision, then prove that behavior-bearing source/configuration drift invalidates the qualified production route rather than inheriting stale provider evidence?